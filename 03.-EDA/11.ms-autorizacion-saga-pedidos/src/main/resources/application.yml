spring:
  application:
    name: ms-autorizacion-saga-pedidos
  
  config:
    import: optional:configserver:http://localhost:8888

#  cloud:
#    loadbalancer:
#      health-check:
#       interval: 15s
feign:
  hystrix:
    enabled: false
